## 0.10.1 · Material 3 Expressive

- 重绘双色对话图标：浅鸢尾紫底、深紫与青绿气泡，优化小尺寸识别与留白。
- 同步更新自适应图标、Android 13 主题单色图标和模块市场图标。
- 常驻通知使用同源单色图标；折叠时优先显示客户端连接数量与端口，展开后显示账号与在线时长。保留点击打开 QQ、唤醒锁操作和静默通知行为。
- 等待登录时的文案区分本地服务是否就绪，避免误报「服务就绪」。
- 模块仍为后台服务，没有新增启动器界面；保留普通包和 stealth 包，沿用原签名覆盖升级。

验证：20 项 JVM 测试、原生过滤测试、两种 APK 构建与清单检查通过；SVG、Android 矢量和通知图标源码同步检查通过。消息协议与内核功能保持现有实现。

真机覆盖安装后，新进程 `/healthz` 返回 `version: 0.10.1`、`online: true`、`listening: true`，客户端已重新连接；系统确认新通知已发布且保留唤醒锁操作。
