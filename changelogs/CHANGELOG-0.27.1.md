## 0.27.1 · 知弦

### 协议

- 收到的图片带上 QQ 的图片子类型：收藏/自定义表情在 `img` 上多一个 `sub-type="1"`，另带 `summary`。以前表情包和截图收到的都是同样一个光秃秃的 `<img>`，客户端分不出来。
- 发送时 `<img sub-type="1">` 按表情的样子发（`picSubType = 1`，摘要缺省为「[动画表情]」），把群友的表情包再发一遍不再变成一张带相框的大图。普通图片照旧。
