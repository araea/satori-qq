## 0.8.9.16

- 新增内部只读动作 `message_search`：在单个会话的近期 QQ 本地历史中按关键词、发送者与时间范围搜索，扫描范围有硬上限并支持续查。
- 新增内部只读动作 `group_anniversary`：提供近期入群周年日历、周年数和倒计时，正确处理设备时区与 2 月 29 日。
- 新增内部只读动作 `random_team`：按指定成员、角色或近期活跃度生成安全随机且人数均衡的分队结果。
- 修正引用消息 ID：优先使用 QQ NT `msgId`，并兼容旧版本曾发出的 `msgSeq` 引用。
- 反检测不扩大拦截范围；新增主进程 / MSF 安装覆盖快照，并将环境报告统计落盘节流到最多每 5 秒一次。
- `internal/status` 在总反检测开关关闭时不再错误报告子功能为启用。
