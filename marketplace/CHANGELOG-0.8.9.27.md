## 0.8.9.27

修复复读消息在 ayjx 与 satori-qq 出站队列中等待后，因消息顺序变化而延迟发送旧内容的问题。

- `message.create` 支持短时效条件：复读请求在排队、媒体转换和交给 QQ 内核前都会重新检查。
- 同一频道出现新消息或超过有效期时，旧复读返回空数组并跳过发送，不触发出站熔断。
- 修复事件推送与复读状态更新的并发顺序，避免“哈哈”在后续“是吧还可以吧”之后才出现。
- 新增实现端时效条件回归测试，并保持普通消息发送行为不变。
